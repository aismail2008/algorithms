package com.code.test.problemset.CrackingCodingInterview6.ch10;

import com.code.test.problemset.leetcode.Q16_MergeSortedArray;

/**
 * Given two sorted integer arrays A and B, merge B into A as one sorted array.
 * Note: You may assume that A has enough space to hold additional elements from B. The number of elements initialized in A and B are m and n respectively.
 */
public class Q1_SortedMerge {
    public static void main(String[] args) {
        Q16_MergeSortedArray.merge(new int[]{}, 0, new int[]{}, 0);
    }
}
